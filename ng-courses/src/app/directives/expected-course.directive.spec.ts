import { ExpectedCourseDirective } from './expected-course.directive';

describe('ExpectedCourseDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpectedCourseDirective();
    expect(directive).toBeTruthy();
  });
});
